<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>🐱Facts</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Gloria+Hallelujah&family=Titan+One&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="../public/main.css">
</head>
<body>
    <div class="container">
        <header>
          <h1>Random Cat Fact</h1>
          <p class="subtitle">Learn something new about cats!</p>
          <h3>reload to know new facts</h3>
        </header>
        <div ><img src="../public/images/cat.jpg" alt="cat" class="img"></div>
        <div class="card">
          <!-- Display the random cat fact fetched from the API -->
          <p id="catFactText"><%=content %></p>
          <!-- the content is the js object which contains the cat facts(result.data.facts) -->

          <!-- Display the length of the cat fact (number of characters) -->
          <p >fact length <%= length %></p>
          <!-- the length is js object which contains the cat facts length(result.data.length) -->
        
     </div>
     
     </div>
      
</body>
</html>